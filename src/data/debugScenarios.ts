import type { DebugScenario } from '../types';

export const debugScenarios: DebugScenario[] = [
  {
    id: 'debug-1',
    title: 'Login Returns 401 with Valid Credentials',
    clientReport: 'I keep getting "401 Unauthorized" when I try to log in, but I know my username and password are correct. I just reset my password and it\'s still not working.',
    context: 'You\'re working on the authentication flow. The login form sends credentials to POST /api/auth/login. The API team says the endpoint works fine on their end.',
    actions: [
      {
        id: 'action-1-1',
        label: 'Check Console & Network Tab',
        description: 'Open Chrome DevTools and look at the Console and Network tabs during the login attempt.',
        outcome: 'You spot a 401 response in the Network tab. Expanding the request reveals the body format is wrong — the API expects `{ "email": "...", "password": "..." }` but the frontend sends `{ "username": "...", "password": "..." }`. You fix the key name and login works.',
      },
      {
        id: 'action-1-2',
        label: 'Reset Password Again',
        description: 'Assume the user made a typo and trigger another password reset.',
        outcome: 'The password resets successfully, but the login still returns 401. The issue isn\'t the password — it\'s how the request is structured. You\'ve wasted time.',
      },
      {
        id: 'action-1-3',
        label: 'Check Auth Provider Config',
        description: 'Dive into the Auth0 / NextAuth configuration files.',
        outcome: 'The auth provider config looks correct. The problem isn\'t in the provider setup — it\'s a mismatch in the login API request payload.',
      },
      {
        id: 'action-1-4',
        label: 'Ask Backend to Check Logs',
        description: 'Message the backend team to investigate server-side logs.',
        outcome: 'The backend team confirms the request reaches the server but the `username` field is unrecognized — the endpoint expects `email`. You could have found this yourself in the Network tab.',
      },
    ],
    correctActionId: 'action-1-1',
    explanation: 'The Network tab is the first place to check for API issues. Expanding the failed request showed the request payload was sending `username` instead of `email`, which the API expected. Always verify the actual request payload matches the API contract before blaming credentials.',
    xpReward: 50,
  },
  {
    id: 'debug-2',
    title: 'API Call Fails with CORS Error',
    clientReport: 'I\'m trying to fetch data from our analytics service but the request fails. The console says something about CORS. I didn\'t change anything on the frontend.',
    context: 'The app fetches from https://analytics.example.com/api/v1/stats. This worked in development but is failing now that the frontend is deployed to staging.',
    actions: [
      {
        id: 'action-2-1',
        label: 'Check Network Tab',
        description: 'Open the Network tab and inspect the failed request headers.',
        outcome: 'You see the preflight OPTIONS request returns a missing `Access-Control-Allow-Origin` header. The staging domain isn\'t in the API\'s allowed origins list. You add the staging URL to the API server\'s CORS configuration and the issue is resolved.',
      },
      {
        id: 'action-2-2',
        label: 'Restart Dev Server',
        description: 'Restart the frontend development server to clear any cached state.',
        outcome: 'The restart does nothing — CORS errors are server-side policy issues, not client-side caching problems. The error persists.',
      },
      {
        id: 'action-2-3',
        label: 'Check .env File',
        description: 'Look for a missing or incorrect environment variable for the analytics API URL.',
        outcome: 'The URL is correct and the env var is set properly. The issue has nothing to do with environment variables — it\'s a CORS policy configuration on the analytics server.',
      },
    ],
    correctActionId: 'action-2-1',
    explanation: 'CORS errors are always server-side. The Network tab shows the preflight OPTIONS request and which origins are allowed. The staging domain simply wasn\'t registered in the analytics API\'s CORS allowlist — a quick server config fix resolved it.',
    xpReward: 50,
  },
  {
    id: 'debug-3',
    title: 'App Crashes on Load — "Cannot read property of undefined"',
    clientReport: 'The app is completely blank when I open it. A white screen and nothing loads. This started happening after the last deploy.',
    context: 'A new landing page component was merged yesterday. The error only occurs on the homepage route. No changes were made to the router or global providers.',
    actions: [
      {
        id: 'action-3-1',
        label: 'Check Console for Stack Trace',
        description: 'Open the browser console and look at the full error stack trace with line numbers.',
        outcome: 'The console shows `TypeError: Cannot read properties of undefined (reading \'name\')` at `LandingHero.tsx:42`. You open that file and see the component tries to access `feature.name` where `feature` is `undefined`. The API data for features hasn\'t loaded yet. You add optional chaining and a loading state — crash fixed.',
      },
      {
        id: 'action-3-2',
        label: 'Revert the Last Deploy',
        description: 'Roll back the most recent deployment to restore the working version.',
        outcome: 'The app works again, but the new landing page feature is missing and the team has lost a day of progress. You still don\'t know what caused the crash.',
      },
      {
        id: 'action-3-3',
        label: 'Clear Browser Cache',
        description: 'Hard reload and clear all cached data in the browser.',
        outcome: 'The error persists — this is a runtime JavaScript error, not a caching issue. Cache clearing won\'t fix undefined property access.',
      },
      {
        id: 'action-3-4',
        label: 'Check package.json for Missing Dependencies',
        description: 'Look for a missing package that the new component might need.',
        outcome: 'All dependencies are installed correctly. The crash is caused by a logic bug in the component, not a missing library.',
      },
    ],
    correctActionId: 'action-3-1',
    explanation: '"Cannot read property of undefined" errors always include a stack trace with the exact file and line number. Reading the error pointed directly to `LandingHero.tsx:42`, where accessing `feature.name` failed because `feature` was undefined before the async data loaded. Optional chaining (`feature?.name`) and a guard clause fixed it.',
    xpReward: 60,
  },
  {
    id: 'debug-4',
    title: 'Form Submission Does Nothing',
    clientReport: 'I fill out the contact form and click Submit, but nothing happens. No success message, no error — just nothing. The button doesn\'t even show a loading state.',
    context: 'The contact form uses react-hook-form with a submit handler that calls an API endpoint. Other forms on the site work fine.',
    actions: [
      {
        id: 'action-4-1',
        label: 'Check Console for JS Error',
        description: 'Open the console and attempt to submit the form while watching for errors.',
        outcome: 'The console shows `Uncaught TypeError: e.preventDefault is not a function` in `ContactForm.tsx:24`. You open the file and see the submit handler is calling `event.preventDefault()` but the event parameter is shadowed by a form field value. Renaming the parameter fixes the form.',
      },
      {
        id: 'action-4-2',
        label: 'Inspect Network Tab',
        description: 'Check the Network tab to see if the API request is sent.',
        outcome: 'No request is sent at all — the JavaScript error occurs before any network call is made. The error is in the event handling code itself.',
      },
      {
        id: 'action-4-3',
        label: 'Add console.log to Submit Handler',
        description: 'Insert a console.log at the top of the onSubmit function to see if it runs.',
        outcome: 'The log never appears — the error occurs during event binding, not inside the handler. The handler isn\'t even being called.',
      },
    ],
    correctActionId: 'action-4-1',
    explanation: 'The Console tab catches JavaScript errors before they affect the UI. The error message `e.preventDefault is not a function` revealed a variable shadowing bug — the event parameter was being overwritten by a form field with the same name. Renaming the parameter resolved the silent failure.',
    xpReward: 50,
  },
  {
    id: 'debug-5',
    title: 'Images Not Loading on Page',
    clientReport: 'All the product images on our store page are broken. I see empty boxes with little image icons instead of the actual products. This worked yesterday.',
    context: 'The product images are hosted on a separate CDN at https://cdn.example.com/products/. A new intern deployed changes to the image URL handling last night.',
    actions: [
      {
        id: 'action-5-1',
        label: 'Check Network Tab for 404s',
        description: 'Open the Network tab and filter by "img" to see the image request responses.',
        outcome: 'The image requests are returning 404. The Network tab shows the URLs being requested: `https://cdn.example.com/products//images/product-1.jpg` — there\'s a double slash in the path. The image URL builder concatenates a base URL with a slash and the path already starts with a slash. Removing the extra slash fixes all images.',
      },
      {
        id: 'action-5-2',
        label: 'Clear Browser Cache & Reload',
        description: 'Hard reload the page with cache clearing.',
        outcome: 'The images still fail to load — the requests still return 404. Caching isn\'t the cause of missing assets on the server.',
      },
      {
        id: 'action-5-3',
        label: 'Check Alt Text',
        description: 'Inspect the HTML to verify the img alt attributes.',
        outcome: 'The alt text is correct, which means the images are at least being rendered in the DOM. The issue is with the src URL, not accessibility attributes.',
      },
      {
        id: 'action-5-4',
        label: 'Restart CDN Server',
        description: 'Ask the infrastructure team to restart the CDN service.',
        outcome: 'The CDN is functioning fine — other paths serve correctly. The issue is specifically with the URLs being generated by your frontend code, not the CDN itself.',
      },
    ],
    correctActionId: 'action-5-1',
    explanation: 'The Network tab shows every request the browser makes. Filtering by image type revealed 404 responses with double slashes in the URL path. A quick look at the image URL builder showed a concatenation bug — always check the actual URLs being requested when assets fail to load.',
    xpReward: 50,
  },
  {
    id: 'debug-6',
    title: 'State Update Not Reflecting in UI',
    clientReport: 'I click the "Like" button on a post, the heart icon fills in for a split second, but then immediately reverts to empty. The counter also goes back to the original number.',
    context: 'The like button dispatches an action to update a `likedPosts` state in a React context. The API call succeeds (returns 200 with the updated post).',
    actions: [
      {
        id: 'action-6-1',
        label: 'Check if State is Set Correctly',
        description: 'Add a breakpoint or console.log in the reducer/setter to see what value is being stored.',
        outcome: 'You find that the reducer sets `likedPosts` to the API response body, but the API returns `{ post: { ... } }` while the reducer expects the flat post object. So `setLikedPosts(response.data.post)` should be `setLikedPosts(response.data)`. Fixing the data mapping preserves the like state.',
      },
      {
        id: 'action-6-2',
        label: 'Check API Response in Network Tab',
        description: 'Inspect the API response to see if the server is saving the like.',
        outcome: 'The API returns 200 with `{ post: { likes: 12, likedByUser: true } }` — the server is correct. The problem is on the frontend state management side.',
      },
      {
        id: 'action-6-3',
        label: 'Rebuild the Node Modules',
        description: 'Delete node_modules and reinstall all dependencies.',
        outcome: 'Reinstalling packages has no effect on state management logic. The bug is in application code, not dependencies.',
      },
    ],
    correctActionId: 'action-6-1',
    explanation: 'When the UI reverts immediately, it usually means the state update set the wrong value and something re-rendered with the correct-but-stale value. Adding a log in the reducer showed the API response was wrapped in a `post` property that the reducer didn\'t unwrap, so it stored `undefined` and the previous state was restored.',
    xpReward: 60,
  },
  {
    id: 'debug-7',
    title: 'Environment Variable Missing in Production',
    clientReport: 'The payment page is broken on the production site. It loads but shows "Configuration Error" instead of the payment form. It works perfectly on my local machine.',
    context: 'The payment page uses `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`. The project is deployed on Vercel. The `.env.local` file has the key set correctly for local development.',
    actions: [
      {
        id: 'action-7-1',
        label: 'Check Vercel Dashboard',
        description: 'Go to the Vercel project dashboard and check Environment Variables under Settings.',
        outcome: 'You find that `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` is missing from the production environment. The variable was only set in the preview environment. You add it to the production environment and redeploy — the payment page works.',
      },
      {
        id: 'action-7-2',
        label: 'Redeploy Without Changes',
        description: 'Trigger a fresh deployment from the same commit.',
        outcome: 'The redeploy produces the same result — the env var still isn\'t available at runtime. The variable needs to be configured in Vercel\'s dashboard, not just included in the build.',
      },
      {
        id: 'action-7-3',
        label: 'Check the gitignore File',
        description: 'Verify .env.local is in .gitignore and the key isn\'t committed.',
        outcome: 'The setup is correct — `.env.local` is gitignored. That\'s expected for local-only variables, but production env vars must be set in the deployment platform directly.',
      },
      {
        id: 'action-7-4',
        label: 'Hardcode the Key Temporarily',
        description: 'Put the Stripe key directly in the source code to test if it resolves the issue.',
        outcome: 'It works, but now the secret key is exposed in source control. This is a security risk and not a real fix — you still need to properly configure the env var on Vercel.',
      },
    ],
    correctActionId: 'action-7-1',
    explanation: 'Environment variables in `.env.local` are only for local development. Vercel (and other platforms) requires you to explicitly set env vars in their dashboard or CLI. The `NEXT_PUBLIC_` prefix only makes the variable accessible to the browser — it still needs to be defined in the deployment environment.',
    xpReward: 60,
  },
  {
    id: 'debug-8',
    title: 'Infinite Re-render Loop',
    clientReport: 'The app is running super slow and my browser tab is becoming unresponsive. The CPU usage is pegged at 100%. I just added a search bar component.',
    context: 'A new search component was added to the dashboard. It uses a `useEffect` that fetches results whenever the search query changes. The parent component also re-renders the search component frequently.',
    actions: [
      {
        id: 'action-8-1',
        label: 'Check Console for Warnings',
        description: 'Open the console and watch for React warnings or errors.',
        outcome: 'The console shows the warning: "Warning: Maximum update depth exceeded. This can happen when a component calls setState inside useEffect, but useEffect either doesn\'t have dependency arrays, or one of the dependencies changes on every render." You inspect the SearchBar component and find `setQuery` is called inside a `useEffect` with `query` in the dependency array — creating an infinite loop. Adding proper dependencies and a debounce fixes it.',
      },
      {
        id: 'action-8-2',
        label: 'Check Task Manager',
        description: 'Open the OS task manager to identify which process is consuming CPU.',
        outcome: 'The browser tab is using high CPU, but this tells you nothing about why. React provides explicit warnings for render loop issues — the console is the right place to look.',
      },
      {
        id: 'action-8-3',
        label: 'Remove the SearchBar Component',
        description: 'Comment out the new SearchBar to see if performance returns.',
        outcome: 'Performance returns to normal, but now the search feature is missing. You still don\'t know what caused the issue or how to fix it properly.',
      },
      {
        id: 'action-8-4',
        label: 'Upgrade Node.js Version',
        description: 'Install the latest LTS version of Node.js.',
        outcome: 'Node.js version has nothing to do with browser-side React render loops. This doesn\'t help.',
      },
    ],
    correctActionId: 'action-8-1',
    explanation: 'React explicitly warns about infinite re-render loops in the console. The warning message even describes the common causes. In this case, a `useEffect` was calling `setQuery(query)` where `query` was both the state variable and the dependency, causing React to re-render → re-run the effect → set state again → re-render in an endless cycle.',
    xpReward: 60,
  },
  {
    id: 'debug-9',
    title: 'API Returns Data But UI Shows Nothing',
    clientReport: 'The user profile page is blank. I checked the Network tab and the API returns a 200 with what looks like data, but the page shows nothing. No errors in the console either.',
    context: 'The ProfilePage component fetches a user from `GET /api/users/me`. The response shows in the Network tab as a 200 with a JSON body. The component was recently refactored to use a new data fetching hook.',
    actions: [
      {
        id: 'action-9-1',
        label: 'Check Data Structure vs Expectations',
        description: 'Compare the API response structure with what the component expects by logging the response or inspecting the Network tab response preview.',
        outcome: 'The API returns `{ "user": { "name": "Alice", "email": "alice@test.com" } }` but the component expects `{ "name": "Alice", "email": "alice@test.com" }` at the top level (no `user` wrapper). The new hook unwraps differently than the old one. You update the data mapping — the profile renders correctly.',
      },
      {
        id: 'action-9-2',
        label: 'Clear Site Data',
        description: 'Clear all site data including localStorage, cookies, and IndexedDB.',
        outcome: 'The profile page is still blank. No data is stored client-side for this feature — it\'s fetched fresh on every page load.',
      },
      {
        id: 'action-9-3',
        label: 'Check CSS for Hidden Elements',
        description: 'Inspect the HTML to see if the profile data is rendered but hidden by CSS.',
        outcome: 'The profile section exists in the DOM but is empty — no child elements are rendered. This is a data binding issue, not a CSS problem.',
      },
    ],
    correctActionId: 'action-9-1',
    explanation: 'A 200 response with no UI is often a data structure mismatch. The Network tab\'s "Preview" shows the exact JSON the API returns. Comparing it to the component\'s expected shape revealed a nested `user` key that the component didn\'t account for — a common issue after backend or hook refactors.',
    xpReward: 60,
  },
  {
    id: 'debug-10',
    title: 'Build Fails on Deployment',
    clientReport: 'The deployment failed! I pushed my changes and the CI pipeline shows a red X. The build log says "failed" but I don\'t understand what it means. It worked fine when I ran it locally.',
    context: 'You added a new utility function and a few imports. The project uses TypeScript with Vite. The build fails during the `npm run build` step in CI.',
    actions: [
      {
        id: 'action-10-1',
        label: 'Check Build Logs for Syntax Error',
        description: 'Scroll through the full CI build logs and look for the specific error message with a file path and line number.',
        outcome: 'The build log shows `src/utils/helpers.ts:15:19 - Error: Module \'"../types"\' has no exported member \'UserRole\'.` Your local build succeeded because you had a stale `node_modules` cache. The import path uses `../types` but the file is `src/types/index.ts` — the correct path should be `../../types`. Fixing the import makes the build pass.',
      },
      {
        id: 'action-10-2',
        label: 'Re-run the CI Pipeline',
        description: 'Click "Re-run" on the failed CI job without any code changes.',
        outcome: 'The build fails again with the same error. CI is deterministic — re-running won\'t fix a real source code issue.',
      },
      {
        id: 'action-10-3',
        label: 'Check Node.js Version in CI',
        description: 'Compare the Node.js version used locally vs the version in the CI config.',
        outcome: 'The Node versions match. The error is a TypeScript compilation error, not a runtime version incompatibility.',
      },
      {
        id: 'action-10-4',
        label: 'Commit and Push Again',
        description: 'Make a trivial whitespace change, commit, and push to trigger a new CI run.',
        outcome: 'Same build failure. The CI pipeline works correctly — it\'s catching a real import path error in your code.',
      },
    ],
    correctActionId: 'action-10-1',
    explanation: 'Build logs always include the exact error with file path and line number. CI failed while local builds passed because of stale node_modules — the local compiler had cached the old type definitions. Always scroll through the full CI log instead of immediately assuming an environment issue. The import path `../types` was wrong for the file\'s location in the `utils/` subdirectory.',
    xpReward: 60,
  },
];
